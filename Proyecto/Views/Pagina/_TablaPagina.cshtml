@using Proyecto.Models
@model List<PaginaCLS>
<table class="table">
    <tr>
        <th>
            @Html.LabelFor(model => Model[0].iidpagina)
        </th>
        <th>
            @Html.LabelFor(model => Model[0].mensaje)
        </th>
        <th>
            @Html.LabelFor(model => Model[0].accion)
        </th>
        <th>
            @Html.LabelFor(model => Model[0].controlador)
        </th>
        <th>
            Operaciones
        </th>

    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.iidpagina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mensaje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.accion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.controlador)
            </td>
            <td>
                <button type="button" onclick="Editar(@item.iidpagina);" class="btn btn-primary glyphicon glyphicon-edit" data-toggle="modal" data-target="#exampleModalScrollable"></button>
            </td>
        </tr>
    }

</table>

